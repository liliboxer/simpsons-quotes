const initialState = {
  quotes: [],
  loading: false,
  error: null
};

export default function simpsonsReducer(state = initialState, action) {
  switch(action.type) {
    default: 
      return state;
  }
}
